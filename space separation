Write a program to get n number of values in separate line for set and print the values with space separation.
Sample Input:
5
3
1
4
5
2
Sample Output:
1 2 3 4 5 
Note: There is trailing space at the end of output


answer:
def get_and_print_set():
    n = int(input("10 "))
    elements = set()
    for _ in range(n):
        element = int(input())
        elements.add(element)
    
    # Print elements with trailing space
    for element in sorted(elements):
        print(element, end=" ")
    print()  # Ensuring there's a new line at the end

get_and_print_set()


output:
10  2
 4
 6
4 6 
